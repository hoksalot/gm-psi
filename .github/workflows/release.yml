name: "Create GitHub Release"

on:
  push:
    tags:
      - 'v**'

jobs:
  tagged-release:
    runs-on: "ubuntu-latest"
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v3

      - name: "Create zip"
        run: |
          zip -r player_status_icons.zip lua

      - uses: "marvinpinto/action-automatic-releases@latest"
        with:
          repo_token: "${{ secrets.GITHUB_TOKEN }}"
          prerelease: false
          files: |
            *.zip